# Vendor

This directory contains pinned upstream dependencies.

For Spec-Kit:

```bash
git submodule add https://github.com/github/spec-kit.git vendor/spec-kit
git submodule update --init --recursive
```
